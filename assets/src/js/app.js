import 'bootstrap';
import 'slick-carousel';

(function ($) {
	// On Dom Ready
	$(function () {
		console.log('Dom loaded...');
	});

	// After page loaded completely
	$(window).on('load', function () {});
})(jQuery);
